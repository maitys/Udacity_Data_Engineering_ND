$ which aws
$ aws --version
$ aws configure list-profiles
$ aws iam list-users --profile default

IAM User - smaity1

# aws configure set <varname> <value> [--profile profile-name]
 aws configure set default.region us-east-2
 
# Create pem key and convert to ppk (only once). save in a folder
 
# Create EMR cluster from UI

# Once complete check in UI if cluster is ready or 
$ aws emr describe-cluster --cluster-id j-ZBSVLWV7GZAL --profile default

# Once ready enable the Security Groups setting of the master EC2 instance to accept incoming SSH protocol from your local computer
EC2 --> Security Groups --> Select Security Group associated with Master
Edit the security group to authorize inbound SSH traffic (port 22) from your local computer.

# SSH through putty
# Setup FoxyProxy Extension

# Goto
http://ec2-54-87-217-101.compute-1.amazonaws.com:18080/
http://ec2-54-227-23-55.compute-1.amazonaws.com:18080/
# You should see Spark UI

# Create EMR Jupyter notebook test

Setup Complete
